ALWAYS (
  FORALL u, s, a, l.
    withdraw(u, a)
    AND:L
    s <- SUM(a; u; ONCE[0,31)(withdraw(u, a) AND tp(t)))
    AND:L
    ((NOT ((ONCE limit(u, l)) AND (EXISTS l2. limit(u, l2)))) SINCE limit(u, l))
    IMPLIES:L
    s <= l
)