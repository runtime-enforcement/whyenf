ALWAYS (
  FORALL user, data.
    insert(user, "db3", data) AND data <> "[unknown]"
    IMPLIES:R
    (
      ONCE[0,60s) (
        EXISTS user2. insert(user2, "db2", data)
      )
      OR:R
      EVENTUALLY[0,1s] (
        EXISTS user2. insert(user2, "db2", data)
      )
    )
)
