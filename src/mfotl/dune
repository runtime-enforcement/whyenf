(ocamllex
 (modules sig_lexer db_lexer formula_lexer))

(menhir
 (modules sig_parser db_parser formula_parser))
 ;; debug (flags --explain --dump)

(library
 (name mfotl)
 (libraries util core_kernel base zarith)
 (preprocess (pps ppx_jane)))
