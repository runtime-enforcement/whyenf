(ocamllex
 (modules mtl_lexer))

(menhir
 (modules mtl_parser))

(library
 (name src)
 (libraries core_kernel base zarith landmarks)
 (preprocess (pps landmarks-ppx --auto))
 (instrumentation (backend landmarks))
)
